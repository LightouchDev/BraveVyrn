<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>BraveVyrn</title>
    <% if (process.env.NODE_ENV === 'development') { %>
    <script src="http://localhost:8098"></script>
    <% } %>
  </head>
  <body>
    <div id="app"></div>
    <script>
      // global hack for cross environment support
      window.global = window
    </script>

    <!-- webpack builds are automatically injected -->
  </body>
</html>
